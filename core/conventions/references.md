# Reference Rules

Reference priority:
1. manifest id
2. canonical relative path
3. report artifact path
4. archive path only when active content no longer exists

Rules:
- reference the canonical source once, then avoid repeating its content
- prefer module-level links over paragraph-level narrative duplication
- do not cite archive content when current active content exists
- do not make reports a required dependency for core definitions
